AIONCoreManifest-AI-Video/
├── qemu-device/
│   └── cogniforge-vx.c          # QEMU PCI device, BARs, doorbell (renamed from prometheus.c)
├── sm-emulator/
│   ├── sm_maud.c                # Virtual SM, warp fibers, MAUD execution
│   └── sm_maud.h
├── tensor-jit/
│   ├── tensor_jit_avx512.c      # AVX‑512/BF16 micro‑kernel generator
│   └── jit_cache.h
├── cognimesh/
│   ├── cognimesh_atomic.c       # RDMA atomicAdd, InfiniBand verbs dispatch
│   ├── cognimesh_init.c         # Mesh fabric setup
│   └── cognimesh.h
├── drivers/
│   ├── linux/
│   │   └── cogniforge.ko        # kernel‑mode DRM driver (NVIF/OpenGPU compatible)
│   └── windows/
│       └── cogniforge.sys       # WDDM driver (placeholder)
├── vulkan-icd/
│   └── cogniforge_icd.json      # Vulkan ICD manifest routing to our backend
├── rack-blueprint/
│   └── rack_plan.md             # Physical rack build guide, BOM, cabling
└── README.md                    # Full vision, build instructions, contribution